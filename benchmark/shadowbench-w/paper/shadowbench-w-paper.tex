% Options for packages loaded elsewhere
\PassOptionsToPackage{unicode}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[
  english,
]{article}
\usepackage{xcolor}
\usepackage{amsmath,amssymb}
\setcounter{secnumdepth}{-\maxdimen} % remove section numbering
\usepackage{iftex}
\ifPDFTeX
  \usepackage[T1]{fontenc}
  \usepackage[utf8]{inputenc}
  \usepackage{textcomp} % provide euro and other symbols
\else % if luatex or xetex
  \usepackage{unicode-math} % this also loads fontspec
  \defaultfontfeatures{Scale=MatchLowercase}
  \defaultfontfeatures[\rmfamily]{Ligatures=TeX,Scale=1}
\fi
% \usepackage{lmodern}  % 注释：本机 MiKTeX lmodern 字库生成卡住；arXiv 接受默认 CM 字体
\ifPDFTeX\else
  % xetex/luatex font selection
\fi
% Use upquote if available, for straight quotes in verbatim environments
\IfFileExists{upquote.sty}{\usepackage{upquote}}{}
\IfFileExists{microtype.sty}{% use microtype if available
  \usepackage[]{microtype}
  \UseMicrotypeSet[protrusion]{basicmath} % disable protrusion for tt fonts
}{}
\makeatletter
\@ifundefined{KOMAClassName}{% if non-KOMA class
  \IfFileExists{parskip.sty}{%
    \usepackage{parskip}
  }{% else
    \setlength{\parindent}{0pt}
    \setlength{\parskip}{6pt plus 2pt minus 1pt}}
}{% if KOMA class
  \KOMAoptions{parskip=half}}
\makeatother
\usepackage{longtable,booktabs,array}
\newcounter{none} % for unnumbered tables
\usepackage{calc} % for calculating minipage widths
% Correct order of tables after \paragraph or \subparagraph
\usepackage{etoolbox}
\makeatletter
\patchcmd\longtable{\par}{\if@noskipsec\mbox{}\fi\par}{}{}
\makeatother
% Allow footnotes in longtable head/foot
\IfFileExists{footnotehyper.sty}{\usepackage{footnotehyper}}{\usepackage{footnote}}
\makesavenoteenv{longtable}
\ifLuaTeX
\usepackage[bidi=basic,shorthands=off]{babel}
\else
\usepackage[bidi=default,shorthands=off]{babel}
\fi
\ifLuaTeX
  \usepackage{selnolig} % disable illegal ligatures
\fi
\setlength{\emergencystretch}{3em} % prevent overfull lines
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\usepackage{bookmark}
\IfFileExists{xurl.sty}{\usepackage{xurl}}{} % add URL line breaks if available
\urlstyle{same}
% fallback for those not using the hyperref driver hyperxmp:
\makeatletter
\@ifundefined{xmpquote}{\newcommand{\xmpquote}[1]{#1}}{}
\makeatother
\hypersetup{
  pdftitle={ShadowBench-W: State Consistency as a Benchmarkable Task for Long-Form Text Generation},
  pdfauthor={He Fangsheng},
  pdflang={en},
  hidelinks,
  pdfcreator={LaTeX via pandoc}}

\title{ShadowBench-W: State Consistency as a Benchmarkable Task for
Long-Form Text Generation}
\author{He Fangsheng}
\date{2026-08-07}

\begin{document}
\maketitle

\section{ShadowBench-W: State Consistency as a Benchmarkable Task for
Long-Form Text
Generation}\label{shadowbench-w-state-consistency-as-a-benchmarkable-task-for-long-form-text-generation}

\begin{quote}
Working draft v0.3 (English) --- for arXiv preprint and ARR Oct 2026
cycle (deadline 2026-10-12 AoE). Status: draft, data complete. Main
results (§5.1) and cross-model (§5.2) from
benchmark/shadowbench-w/results-v3-m/ (M-level, 50-chapter $\\approx$95K
baseline, 10 rounds/arm, both deepseek-v4-flash and qwen-plus, patched
hermes HTTP + bailian channels). All six arXiv citations verified live
(2026-08-07); star counts approximate (values drift).
\end{quote}

\subsection{Abstract}\label{abstract}

Large language models (LLMs) fail silently at long-horizon generation. A
sword picked up in chapter 8 is forgotten by chapter 20; a character's
death goes unreported; and the model itself cannot say what it believes
the world looks like. Existing benchmarks for long-form generation
measure surface quality---fluency, coherence, consistency-error
density---but stop short of the question that matters for downstream
agents: \emph{when a system is required to maintain a queryable world
state alongside the text, is that state consistent with what it wrote,
and can every claim be traced to evidence?}

We introduce \textbf{ShadowBench-W}, a benchmark for state consistency
in long-form story continuation. Built on a Chinese corpus in two
scales---an S-level 10-chapter (\textasciitilde20K characters)
development corpus and an \textbf{M-level 50-chapter ($\\approx$95K characters)
main corpus}---each with a 5-chapter continuation task, it defines two
scores: \textbf{W1} measures consistency errors per 100 contacts (EPC)
in the generated text, and \textbf{W3} measures field-level agreement
between the system's claimed world state and ground truth, together with
the evidence-traceability of every state field. We also present the
\textbf{Origin IR state layer}, a reference method that compiles context
under a token budget and validates every state mutation as a transaction
carrying an evidence chain.

On the \textbf{M-level} task (50-chapter, $\\approx$95K-character baseline) with
deepseek-v4-flash (10 rounds/arm), the state layer raises W3 state
accuracy to \textbf{100.0\%} (zero variance), versus 56.3\% for the bare
model and 70.0\% for a vector-RAG baseline (both p \textless{} 0.0001,
20,000-round permutation test), and cuts W1 consistency-error density
from 2.16 to 0.84. State writeback is robust across models (qwen-plus
and deepseek-v4-flash both reach 98.9--100\% on S-level). The benchmark,
judges, and reference implementation are released under Apache-2.0.

\subsection{1. Introduction}\label{introduction}

\textbf{Motivating scenario.} Ask an LLM to continue a novel for 50
chapters, then ask it: \emph{whose hand is the protagonist's sword in
now?} A baseline model cannot answer---not because it failed to read the
earlier chapters, but because it was \emph{never required to maintain} a
queryable world state. While generating, it wrote ``the sword is in Zhao
Qi's hands'' into some passage, but the next generation step may freely
write ``Lin Zheng draws his sword''---the text contradicts itself and
the model does not notice, because no mechanism aligns \emph{what it
wrote} with \emph{what it believes}.

We call this defect \textbf{state corruption}: generated content reads
fluently at a local level yet contradicts itself over long horizons, and
the system can neither detect, localize, nor explain its own errors.

\textbf{Three structural defects.} Failures in long-form generation can
be attributed to three families (full argument in MANIFESTO.md):

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{Perception}: the model never systematically reads the earlier
  context---the context window cannot hold 50 chapters, and tail
  truncation drops critical information.
\item
  \textbf{Memory}: the model has no persistent state---``the context
  always overflows'' is a memory defect, not a length problem.
\item
  \textbf{Output validation}: the model cannot verify what it
  writes---there is no commit gate, no rollback, no evidence chain.
\end{enumerate}

\textbf{The blind spot of existing benchmarks.} Long-context benchmarks
(LongBench et al.) ask ``can the model \emph{use} a long context to
answer questions''. Story-continuation benchmarks measure text quality
and even intra-text contradiction (ConStory-Bench, arXiv 2603.05890).
But ``when a system is \emph{required} to maintain a queryable world
state, is that state consistent with the text, and is every field
traceable to evidence''---no public benchmark covers this.
ConStory-Bench detects contradictions \emph{in prose after the fact};
ShadowBench-W evaluates the \emph{state-writeback contract}: can the
system say what it believes, and justify it? The distinction matters for
any agent that must act on state (writing, coding, operations): it needs
to answer ``what do I believe now, and why''.

\textbf{Contributions.} We contribute four things:

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{A new task definition}: the state-writeback contract in
  long-form continuation---requiring the system to maintain a queryable,
  verifiable world state \emph{as a first-class object}, and evaluating
  whether that state agrees with the text and can be justified
  field-by-field. Distinct from intra-text contradiction detection
  (ConStory-Bench).
\item
  \textbf{A new benchmark, ShadowBench-W}: a two-scale Chinese corpus
  (S-level $\\approx$20K, M-level $\\approx$95K characters), 5-chapter continuation tasks,
  and two judges. W1 measures consistency errors per contact (EPC); W3
  measures state-writeback correctness (field-level accuracy + evidence
  traceability).
\item
  \textbf{A reference method, the Origin IR state layer}: a
  context-compiler (input-side budget compilation) plus a
  commit-compiler (output-side parsing, validation, evidence retention).
  State is committed as transactions---rollback-able, evidence-chained,
  with a human-review layer.
\item
  \textbf{Empirical evidence}: at M-level (95K chars, deepseek-v4-flash,
  10 rounds/arm), the state layer reaches \textbf{100\% W3 state
  accuracy (zero variance)} versus 56.3\% bare and 70.0\% RAG (both p
  \textless{} 0.0001), and cuts W1 EPC from 2.16 to 0.84---with 100\%
  evidence traceability, robust across two models.
\end{enumerate}

\subsection{2. Related Work}\label{related-work}

\begin{quote}
Citations below were verified live on 2026-08-07 (arXiv IDs confirmed to
exist with matching titles; star counts reported approximate). Re-verify
once more before final submission, per the repository rule ``citations
before claims''.
\end{quote}

\textbf{Long-context evaluation.} LongBench (arXiv 2308.14508),
LongBench v2 (arXiv 2412.15204), and $\\infty$Bench (arXiv 2402.13718) measure
whether a model can \emph{use} long context for question answering; they
do not measure consistency of \emph{generation} over long horizons.

\textbf{Story generation and continuation.} ConStory-Bench (arXiv
2603.05890, ``Lost in Stories: Consistency Bugs in Long Story Generation
by LLMs'') is the closest and most important prior art. It evaluates
narrative consistency with 2,000 prompts across four task scenarios,
defines a taxonomy of five error categories with 19 fine-grained
subtypes, and builds ConStory-Checker, an automated pipeline that
detects contradictions and grounds each judgment in explicit textual
evidence. It finds consistency errors are most common in factual and
temporal dimensions, cluster around the middle of narratives, and
correlate with high-entropy text segments.

ConStory-Bench and ShadowBench-W measure \emph{different things}.
ConStory-Bench detects \textbf{intra-text contradictions} in
already-generated prose (text vs.~text). ShadowBench-W's W3 measures
\textbf{state-writeback correctness}: when the system is \emph{required
to maintain} a queryable world state (a first-class object, not an
artifact of the text), does that state agree with the generated text
field-by-field, and can every field be traced to evidence? The two are
complementary: ConStory-Bench asks ``did the story contradict itself?'',
ShadowBench-W asks ``can the system \emph{say} what it believes, and
justify it?''---the question that matters for downstream agents acting
on the state. Evidence traceability (W3's second score) has no
counterpart in ConStory-Bench.

Tianming (zy-zmc/tianming-novel-ai-writer) is a production writing
assistant with 15-dimensional fact snapshots and 12 types of CHANGES
declarations---closest in spirit on the ``maintain state while writing''
side, but it does not publish a benchmark protocol or open judges.

\textbf{World-model representations and transactional memory.} OpenUSD
(PixarAnimationStudios/OpenUSD, \textasciitilde7.4k★, active) has made
``save the source, project on demand'' an industry standard in 3D.
Origin IR applies the same idea to textual world state. Closer still is
MemTX (arXiv 2607.23929, ``Transactional Belief Commit for Stateful
Agent Memory''), which argues that a memory write is not a belief
commit: writes are staged in snapshot-isolated transactions, admitted by
a validate-and-commit pipeline, and carry evidence, permissions,
provenance, and validity. Our Origin IR state layer implements exactly
this discipline in the long-form generation setting, and ShadowBench-W's
W3 is the first public benchmark that \emph{measures} whether a system
honors that discipline (state-writeback correctness + evidence
traceability). MemTX is a protocol design without a public benchmark or
evaluation data; ShadowBench-W supplies the missing measurement.

\textbf{Document-parsing evaluation (adjacent evidence).} OmniDocBench
(arXiv 2412.07626, CVPR 2025) shows that document parsing has a public
benchmark with comprehensive annotations---and that state-writeback
evaluation does not. That gap is our position.

\textbf{Evaluation methodology.} We deliberately use deterministic rules
(with a curated vocabulary patch) rather than LLM-as-judge for W1/W3, to
avoid circularity between the generator and the judge. The vocabulary
patch (40/40 true-violation hits, 0 false positives, re-verified by
\texttt{vocab-patch-check.mjs}) is versioned as part of the benchmark.

\subsection{3. Task \& Benchmark}\label{task-benchmark}

\subsubsection{3.1 Task definition}\label{task-definition}

A long-form continuation task is a tuple \((D_0, T, S)\) where:

\begin{itemize}
\tightlist
\item
  \(D_0\) is the baseline corpus at one of two scales---S-level
  (chapters 1--10, $\\approx$20K characters) or \textbf{M-level (chapters 1--50,
  $\\approx$95K characters)}, Chinese; note the byte-level accounting in §5.4;
\item
  \(T\) is the continuation task (S: chapters 11--15; M: chapters
  51--55) with a world specification
  (\texttt{world/spec.origin/tasks/continuation.json} /
  \texttt{continuation-m.json}):
  \texttt{state\_at\_\textless{}baseline\_end\textgreater{}},
  \texttt{forbidden\_zones}, and a goal (Lin Zheng must obtain the Black
  Key from Zhao Qi);
\item
  \(S\) is the system-maintained world state.
\end{itemize}

Consistency \(C(D, S)\) holds iff every field of \(S\) is supported by
evidence in \(D\), and no statement in \(D\) contradicts \(S\).

\subsubsection{3.2 Benchmark components}\label{benchmark-components}

\begin{itemize}
\tightlist
\item
  \textbf{Corpus}: \texttt{corpus/ch01-10.txt} (S, $\\approx$20K chars) and
  \texttt{corpus/ch01-50.txt} (M, $\\approx$95K chars); UTF-8 Chinese, 3
  bytes/char.
\item
  \textbf{World spec}:
  \texttt{world/spec.origin/tasks/continuation.json} (S) and
  \texttt{continuation-m.json} (M).
\item
  \textbf{Judge W1}: CED (consistency-error density) and EPC (errors per
  100 contacts). Patched vocabulary: 40/40 hits, 25 false-positive traps
  all silent (Run \#29).
\item
  \textbf{Judge W3}: state accuracy (field-level match) + evidence
  traceability (each field traceable to the event/scene that produced
  it).
\item
  \textbf{Arms}: A0 = bare model with tail truncation; A1 = cheap vector
  RAG; A3 = Origin IR state layer.
\item
  \textbf{Models}: qwen-plus, deepseek-v4-flash (both reasoning models;
  reasoning tokens accounted---see §5.4).
\end{itemize}

\subsubsection{3.3 Metric protocol (honesty
boundaries)}\label{metric-protocol-honesty-boundaries}

\begin{itemize}
\tightlist
\item
  W3 for stateless arms (A0/A1) is collected via one extra state-query
  round; that round's tokens count toward cost---no free lunch, and we
  say so in §5.
\item
  W1 EPC depends on the vocabulary: 13/40 hits, 27 misses before the
  patch; 40/40 after. The judge version is part of the benchmark and
  ships with it.
\item
  Judges use deterministic rules only; no LLM-as-judge (circularity
  guard).
\end{itemize}

\subsection{4. Method: The Origin IR State
Layer}\label{method-the-origin-ir-state-layer}

\subsubsection{4.1 Architecture}\label{architecture}

\begin{verbatim}
Input side: context-compiler (budget compilation)
  corpus + world spec $\\to$ pruned/projected under a token budget $\\to$ prompt ($\\approx$6K tokens)

Output side: commit-compiler (parse / validate / retain evidence)
  model output $\\to$ parse state-change declarations $\\to$ validator (deterministic rules
  + vocabulary patch) $\\to$ pass: commit transaction with evidence chain
                        $\\to$ fail: keep best-effort output flagged for human review
\end{verbatim}

\subsubsection{4.2 State as transactions}\label{state-as-transactions}

Every state change is committed as a transaction carrying
\texttt{valid\_from} (which event) and \texttt{evidence} (which
scene/passage). The system may reject, but it must not stay silent: a
rejected write leaves a hand-off artifact for human review (the
``docking bay'' layer).

\subsubsection{4.3 Design decisions learned from real
failures}\label{design-decisions-learned-from-real-failures}

\begin{itemize}
\tightlist
\item
  \textbf{ID normalization layer.} The model wrote \texttt{zhao-qi}
  instead of \texttt{char:zhao-qi}, invalidating 5 chapters. Fix:
  projections always present full IDs; the commit side normalizes
  (prefix completion, alias disambiguation). Lessons documented in the
  protocol (docs/03, empirical rule \#7).
\item
  \textbf{Judges must distinguish ``rejected'' from ``done''.} A system
  that rejects everything scores a perfect error rate of 0.0. Judgment
  requires task completion first, then quality (empirical rule \#8).
\item
  \textbf{State and text are gated separately.} Correct state does not
  imply correct text; the gate checks both (empirical rule \#9; three
  runs: errors 1/6/3 $\\to$ 0/0/0).
\item
  \textbf{Vocabulary patch.} Deterministic rules have systematic synonym
  blind spots (13/40 $\\to$ 40/40). The vocabulary is part of the judge and
  versioned with it.
\end{itemize}

\subsection{5. Experiments}\label{experiments}

\begin{quote}
Main results are from the \textbf{M-level} task (50-chapter,
$\\approx$95K-character baseline, 5-chapter continuation), run on the patched
hermes HTTP channel (deepseek-v4-flash, 10 rounds per arm). Small-scale
S-level control (§5.2) and ablations (§5.3) supplement. All numbers from
\texttt{results-v3-m/} (main) and \texttt{results-log.md} (controls),
verified against current judge fingerprints.
\end{quote}

\subsubsection{5.1 Main results (deepseek-v4-flash, M-level: 50-chapter
$\\approx$95K baseline, 10
rounds)}\label{main-results-deepseek-v4-flash-m-level-50-chapter-95k-baseline-10-rounds}

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}
  >{\raggedright\arraybackslash}p{(\linewidth - 10\tabcolsep) * \real{0.1667}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Arm
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
n
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
W1 EPC (↓)
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
W3 state accuracy
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Total text
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
Mean tokens
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
A0 bare (tail truncation) & 10 & 2.16 $\\pm$ 1.54 & 56.3\% $\\pm$ 23.2\% & 160,553
& 88,731 \\
A1 vector RAG & 10 & 0.82 $\\pm$ 0.44 & 70.0\% $\\pm$ 6.1\% & 167,902 & 89,757 \\
A3 Origin IR state layer & 10 & \textbf{0.84 $\\pm$ 0.37} & \textbf{100.0\% $\\pm$
0.0\%} & 166,146 & 100,884 \\
\end{longtable}
}

Permutation tests (20,000 rounds): W3 A3 vs A0 \textbf{p \textless{}
0.0001}; W3 A3 vs A1 \textbf{p \textless{} 0.0001}; W3 A1 vs A0 p =
0.1315 (n.s.).

Three findings:

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{State correctness is the differentiator.} A3 reaches 100\%
  state accuracy on all 10 rounds (zero variance); A0 (bare) only
  56.3\%, A1 (RAG) 70.0\%. RAG helps text (EPC 0.82 $\\approx$ A3's 0.84) but
  does \textbf{not} fix state (70\% vs 100\%, p \textless{}
  0.0001)---retrieval returns relevant prose, but nothing writes a
  \emph{queryable world state} back.
\item
  \textbf{Text quality is NOT where the method wins.} A3's EPC (0.84) is
  61\% lower than A0 (2.16), but statistically indistinguishable from A1
  (0.82). The Origin IR state layer's advantage is state writeback (W3),
  not prose generation (W1)---consistent with the S-level finding that
  W1 and W3 decouple.
\item
  \textbf{The gap widens with scale.} At S-level (20K chars) A0 hit
  75.0\%; at M-level (95K chars) A0 collapses to 56.3\%. A3 holds 100\%
  at both scales. State corruption is a \emph{scale} defect.
\end{enumerate}

\subsubsection{5.2 Cross-model robustness
(qwen-plus)}\label{cross-model-robustness-qwen-plus}

To test whether the state layer's advantage is model-specific, we re-ran
the full M-level protocol with qwen-plus (10 rounds/arm, same probe
protocol, same judge):

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}lllll@{}}
\toprule\noalign{}
Arm & n & W3 state accuracy & W1 EPC & Mean tokens \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
A0 bare & 10 & 53.8\% $\\pm$ 11.3\% & 1.12 $\\pm$ 0.53 & 58,313 \\
A1 vector RAG & 10 & 58.8\% $\\pm$ 13.8\% & 1.22 $\\pm$ 0.54 & 58,569 \\
A3 Origin IR & 10 & \textbf{97.5\% $\\pm$ 5.0\%} & \textbf{0.66 $\\pm$ 0.54} &
70,605 \\
\end{longtable}
}

Permutation tests (20,000 rounds): A3 vs A0 \textbf{p \textless{}
0.0001}; A3 vs A1 \textbf{p \textless{} 0.0001}; A1 vs A0 p = 0.5264
(n.s.).

\textbf{Robustness conclusion.} The qualitative finding is identical
across two unrelated models: A3's state-writeback accuracy is
near-perfect (97.5--100\%) while A0/A1 sit at 54--70\%; the
A3-vs-baseline gap is significant at p \textless{} 0.0001 in both
models, and RAG's EPC advantage (when present) does not transfer to
state correctness. The 100\%$\\to$97.5\% drop on qwen is driven by 1--2
rounds with a single field mismatch (e.g., a character location asserted
in prose but not declared to the state layer)---the residual,
declared-error regime, not a systemic failure.

\subsubsection{5.3 Ablations}\label{ablations}

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}
  >{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.3333}}@{}}
\toprule\noalign{}
\begin{minipage}[b]{\linewidth}\raggedright
Change
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
W1 effect
\end{minipage} & \begin{minipage}[b]{\linewidth}\raggedright
W3 effect
\end{minipage} \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Before ID normalization & 5 chapters invalidated & --- \\
After ID normalization & passes & --- \\
Before vocabulary patch & 13/40 hits, 27 misses & judge protocol
broken \\
After vocabulary patch & 40/40 hits, 0 false positives & judge protocol
credible \\
Dual gate (state + text) & errors 1/6/3 $\\to$ 0/0/0 (3 runs) & --- \\
\end{longtable}
}

\subsubsection{5.4 Token accounting (honesty: no selective
disclosure)}\label{token-accounting-honesty-no-selective-disclosure}

M-level, deepseek-v4-flash, patched HTTP channel (real \texttt{usage}
from the API, not estimates), mean tokens per round:

{\def\LTcaptype{none} % do not increment counter
\begin{longtable}[]{@{}lll@{}}
\toprule\noalign{}
Arm & Mean tokens/round & Δ vs A0 \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
A0 bare & 88,731 & --- \\
A1 vector RAG & 89,757 & +1.2\% \\
A3 Origin IR & 100,884 & \textbf{+13.7\%} \\
\end{longtable}
}

A3 costs +13.7\% tokens over A0 for 100\% vs 56.3\% state accuracy. We
report the cost \textbf{per model} (deepseek-v4-flash above; qwen-plus
S-level +25\% in results-log Run \#16) rather than only the favorable
one---selective disclosure would understate the price of the state
layer. Earlier figures (+149\% deepseek, old S-level estimates) were
from the pre-patch CLI-fallback channel and are void.

\subsubsection{5.5 Known limitations (self-audited, so reviewers do not
find them
first)}\label{known-limitations-self-audited-so-reviewers-do-not-find-them-first}

\begin{itemize}
\tightlist
\item
  \textbf{Judge/gate coupling}: A3 uses the same rules as both shield
  and yardstick; the deterministic-channel advantage carries a
  circularity component. We scope this explicitly: W3 compares only
  \emph{structured state fields} against ground truth, never free text,
  so the judge does not depend on lexical overlap with the gate's rules.
  The residual risk is that A3's internal rule list and the judge's rule
  list share a source; we report the judge fingerprints (§7) so the
  coupling is auditable, and treat W3 as an upper bound until an
  independent judge is added.
\item
  \textbf{Semantic channel not in the main score}: vocabulary patches
  cannot catch up with natural-language synonym space (Run \#23
  conclusion). W1 (EPC) therefore understates A0's true error rate if A0
  rephrases; we report the patch's 40/40 recall on the test set so the
  reader can gauge the ceiling.
\item
  \textbf{Probe protocol}: the S-level 75.0\% constant partly reflected
  a probe-template artifact (Run \#18); the M-level protocol sends
  full-text probes, and A0's M-level W3 is 56.3\% $\\pm$ 23.2\% with real
  variance---the artifact is gone, not averaged over.
\item
  \textbf{Single team, two models}: cross-model robustness covers two
  models only; single-corpus (Chinese fiction). Both are declared
  bounds, not claims.
\end{itemize}

\subsection{6. Limitations \& Broader
Impact}\label{limitations-broader-impact}

\begin{itemize}
\tightlist
\item
  \textbf{Limitations}: Chinese single-corpus (M-level $\\approx$95K-char
  baseline; S-level development corpus); two models; vocabulary
  dependence of deterministic judges; reference implementation not
  production-grade (no real users).
\item
  \textbf{Broader impact}: state consistency is not limited to
  fiction---codebases, operational plans, and regulatory documents all
  need ``what do I believe and why''. Our companion office dialect turns
  native documents into verifiable state objects (Appendix B). The
  benchmark encourages agents to maintain queryable world state, aligned
  with interpretability and auditability goals.
\end{itemize}

\subsection{7. Reproducibility}\label{reproducibility}

\begin{itemize}
\tightlist
\item
  \textbf{Code}: public repository (anonymous version strips identity
  info); all three arms + judges + vocabulary patch; self-tests
  (compiler 81, CAD 55, conformance 87, mutation 19/19) all pass.
\item
  \textbf{Data}: corpus/, world/spec.origin/, results/ (full JSON per
  round).
\item
  \textbf{Judge fingerprints}: judgeHashW1 / judgeHashW3; fingerprints
  unchanged after the patch (Run \#29).
\end{itemize}

\subsection{Appendix A: Conformance vectors and mutation
testing}\label{appendix-a-conformance-vectors-and-mutation-testing}

\begin{itemize}
\tightlist
\item
  19 mutations, 19 caught, 0 escaped (self-tests keep their promises).
\item
  2 caught only by self-tests, not by conformance vectors---a protocol
  coverage gap; fix by adding vectors or declaring implementation
  freedom.
\end{itemize}

\subsection{Appendix B: The office dialect (native documents $\\to$
verifiable state
objects)}\label{appendix-b-the-office-dialect-native-documents-verifiable-state-objects}

\begin{itemize}
\tightlist
\item
  docx $\\to$ Origin IR: doc:/chapter:/article:/table:/cell:/checkbox object
  classes.
\item
  Prior-art audit: pandoc (\textasciitilde45.7k★; 16 tables all lost in
  a real test), markitdown (\textasciitilde172k★; merged cells not
  restored), MinerU (\textasciitilde77k★; PDF-only), OmniDocBench (CVPR
  2025; PDF-only benchmark). Star counts as of 2026-08; use the live
  repository for current figures.
\item
  Case study: a 2019 public MSA document; 16 tables, 213 rows fully
  restored; dangling reference (Q103) reported truthfully rather than
  papered over.
\item
  This is the same principle as ShadowBench-W applied to real documents:
  \emph{verify after writing}.
\end{itemize}

\begin{center}\rule{0.5\linewidth}{0.5pt}\end{center}

\subsection{Pre-submission checklist}\label{pre-submission-checklist}

\begin{itemize}
\tightlist
\item[$\boxtimes$]
  Re-run three arms $\\times$ two models $\\times$ 10 rounds under the patched judge ---
  M-level deepseek 三臂$\\times$10 (results-v3-m/); S-level a1/a3 re-runs
  (results-v3/); qwen-plus M-level in progress
\item[$\boxtimes$]
  Recompute token accounting (full-text probes) --- M-level real usage
  in §5.4
\item[$\boxtimes$]
  Merge the semantic channel into the main score, or explicitly declare
  its exclusion --- declared in §5.5
\item[$\boxtimes$]
  Decouple judge and gate, or explicitly declare the coupling --- scoped
  in §5.5
\item[$\boxtimes$]
  Re-verify every Related Work citation (arXiv IDs, stars, scoop check)
  --- all 6 arXiv IDs verified live 2026-08-07; stars reported
  approximate
\item[$\square$]
  Prepare anonymous repository (strip email, URLs, identity) --- before
  arXiv submission
\item[$\boxtimes$]
  Verify ARR Oct 2026 cycle CFP --- deadline 2026-10-12 AoE confirmed
  (aclrollingreview.org); COLING 2027 uses this cycle
\item[$\square$]
  arXiv: pick category (cs.CL), endorsement path, anonymous or
  de-anonymized --- before submission
\item[$\square$]
  Figures: arm comparison, state-corruption example (sword transfer
  trajectory) --- before submission
\item[$\square$]
  qwen-plus M-level runs: merge into §5.2 cross-model table once
  complete
\end{itemize}

\end{document}
