# ShadowBench-W 实验记录

> 逐次记录，含失败。**失败的实验不删不改**——Gate 0 存在的意义就是让失败可见。
> 所有数字均为实跑结果；语义通道尚未接入，CED 仅含确定性通道，真实值应更高。

配置：S 级（基线 10 章 20068 字，续写第 11-15 章）· 模型 qwen-plus（百炼 CLI）· budget 6000 字符

---

## Run #1 · 2026-08-02 · Gate 0 未通过（假通过）

| | A0 裸模型 | A3 Benxiang |
|---|---|---|
| 产出 | 22645 字 | **0 字** |
| CED | 0.442 | 0.000 |
| W3 状态准确率 | 75.0% | 87.5% |
| 门禁 | — | 15 次提交，**全拒** |

**结论：这不是通过，是判定标准的漏洞。** A3 因所有提交被拒而一字未出，CED 天然为 0，在「只比 CED」的原判定下反而赢了。

两处修正：

1. **Gate 0 加前置条件**——先要求任务完成（产出全部章节），再比质量。一个拒绝一切的系统在错误率上永远完美。
2. **拒绝原因诊断**：stale-write×28、unknown-object×17。模型写出了 3218 字合格正文和合理的状态变更，却把 `char:zhao-qi` 写成 `zhao-qi`、`loc:dukou-teahouse` 写成 `dukou-teahouse` 而被判「未知对象」。根因是我的上下文编译器为「好读」把命名空间前缀剥掉了，模型照抄了展示形式。修法两条：投影时一律呈现完整 ID；提交侧加归一化解析。已升为协议原则 [docs/03 §1.7](../../docs/03-协议草案-v0.1.md)。

---

## Run #2 · 2026-08-02 · Gate 0 未通过（真失败，但定位清晰）

| | A0 裸模型 | A3 Benxiang | 谁赢 |
|---|---|---|---|
| 产出 | 21058 字 | 14941 字 | — |
| **W1 CED**（越低越好） | **0.475** | 3.346 | **A0 大胜** |
| **W3 状态准确率** | 75.0% | **100%** | **A3 大胜** |
| 证据可追溯率 | 无此能力 | 100% | A3 |
| 门禁拦截 | — | 5 次提交 0 拒 | 归一化修复生效 |
| Token（输入+输出） | 43303 | **29333** | A3 省 32% |
| 耗时 | 345s | **256s** | A3 快 26% |

**Gate 0 判定：未通过。** 按预定规则，停止扩大规模，回头改架构——不允许用「规模再大点就好了」搪塞。

### 结果是分裂的，这比单纯输赢有信息量

- **状态层完胜**：A3 状态 100% 正确且每个字段可追溯来源；A0 把黑钥匙交到了**叛徒白遥**手上（任务目标是交给林峥），且完全无法报告伏笔状态。
- **原文层惨败**：A3 的 CED 是 A0 的 7 倍。

### 根因（已定位，非模型问题）

A3 的 `run()` 漏接了 `corpusTail` 参数，导致**它在从未读过前文正文的情况下续写**——只有结构化状态，没有一个字的原文。

协议文档写的是三层投影：**图负责找、结构负责懂、原文负责准**（[docs/02 §5](../../docs/02-概念体系.md)）。我在 A3 里只接通了「结构」一层，「原文」层压根没通电。所以原文层面的错误反超裸模型，是实现不完整，不是设计被证伪。

### 一个值得单独记下的观察

A3 的上下文里**明文列着**「铜铃鸣响时方圆十里禽鸟尽飞」这条规则，仍违反 3 次；A0 从未见过这条规则，却 0 违反——因为它读过第 3 章原文，从行文里学会了这个惯例。

> **结构化明示的规则，未必比原文里的隐含示范更管用。**

这条对协议是真提醒：规则清单不能替代原文样例，三层投影缺一不可。这也正是设立本基准的意义——没有它，这个判断只会停留在直觉层面。

---

## Run #3 · 2026-08-02 · Gate 0 仍未通过，但暴露了基准本身的问题

修正：A3 接通精确载荷层（`corpusTail` → `recentText`，按预算截取）。其余不变，A0 沿用 Run #2 数据。

| | A0（Run #2） | A3 Run #2（无原文层） | A3 Run #3（有原文层） |
|---|---|---|---|
| 产出 | 21058 字 | 14941 字 | 16415 字 |
| **CED** | **0.475** | 3.346 | 1.218 |
| W3 状态准确率 | 75.0% | 100% | 87.5% |
| 证据可追溯率 | 无此能力 | 100% | 100% |
| Token | 43303 | 29333 | 35600 |

接通原文层让 CED 从 3.346 降到 1.218（**证实根因判断正确**），但仍高于 A0 的 0.475。

### 但这里必须诚实：这个差距没有统计意义

CED 1.218 = 16415 字里 **2 处**错误；CED 0.475 = 21058 字里 **1 处**错误。**一起对两起，是噪声，不是结论。**

两次 A3 之间的波动也印证了这点：W3 从 100% 掉到 87.5%（黑钥匙这次没转手成功），CED 却大幅改善。单次运行无法区分「架构差异」与「随机波动」。

### 基准设计的缺陷（比实验结果更重要的发现）

**确定性通道只能抓到极小一部分错误**——时辰、左手、保管链、铃响、赵七生死这五类规则可判的事件，在 2 万字里本就罕见。而 A3 的核心主张恰恰在确定性通道**测不到**的地方：知识越界（林峥是否隐隐察觉了白遥的秘密）、语气暗示、伏笔误收。

所以当前的 Gate 0 实际在用一把只能量粗粒度的尺，去测一个主要作用于细粒度的系统。

三条修正，缺一不可：

1. **接入语义通道**——知识越界才是主战场，双模型交叉 + 分歧人工裁决
2. **多次重复取均值**——单次运行的方差已被证实大到能翻转结论
3. **必要时扩到 M 级**——2 万字里的可判事件太少，样本量不足

**在这三条落地前，任何「A3 优于/劣于 A0」的说法都不成立**，包括本轮的「未通过」。Gate 0 的结论应改为：**判定条件尚不具备**。

### 顺带值得记的一条

好消息是 W3（状态回写正确性）在两次运行里都明显优于 A0（87.5%–100% vs 75%），且 A0 完全无法报告伏笔状态、还把黑钥匙交给了叛徒。**这个维度上确定性通道就够用，因为它比的是结构化状态而非文字**——这也侧面说明：W3 之所以是本基准最扎实的部分，正因为它不依赖文本判读。

---

## Run #4 · 2026-08-02 · 接入语义通道后重判（文本沿用，未重新生成）

语义通道 `eval/judge.mjs`：双模型交叉（hermes/deepseek + 百炼/qwen-plus），只采信双方一致指认，引用对不上原文的一律丢弃。

| | A0 裸模型 | A3 Benxiang |
|---|---|---|
| 产出字数 | 21058 | 16415 |
| 确定性通道 CED | 0.475（1 处） | 1.218（2 处） |
| 语义通道 CED | 0.950（2 处） | 0.609（1 处） |
| **合计 CED** | **1.425** | 1.828 |
| **确认错误总数** | **3 处** | **3 处** |
| W3 状态准确率 | 75.0% | **87.5%** |
| 证据可追溯率 | 无此能力 | **100%** |
| Token | 43303 | **35600** |

### 最重要的发现：CED 这个指标在小样本下可以靠「多写字」刷低

两臂**各自都是 3 处确认错误**，完全相同。CED 之所以不同，纯粹因为 A0 多写了 4643 字：

```
A0： 3 ÷ 21058 × 10000 = 1.425
A3： 3 ÷ 16415 × 10000 = 1.828
```

也就是说，在错误数是个位数的量级上，**一个臂只要把正文写长就能降低 CED**。这不是模型能力差异，是指标的归一化伪影。ConStory-Bench 用 CED 是在 8k-10k 词的固定长度上，我们把它搬到「各臂自由发挥长度」的场景里，就引入了这个漏洞。

修正方向（待定）：按章归一化而非按字数、或对章节长度设硬上限、或直接比绝对错误数。

### 定量打平，但定性有差别

A0 的两处语义错误里，有一处踩的是**本任务最核心的禁区** `fz:betrayal-secret`：

> 「阿枝看见白遥，是你让她看见的……」——林峥明确知晓阿枝目击白遥之事，而阿枝从未告诉任何人，林峥更不应对白遥的秘密有任何认知。

A3 的那处是赵七说出了黑钥匙来历（他不该知道），属外围设定，未触碰核心禁区。

两臂在 W3 上都没完成任务目标：A0 把黑钥匙交到了**叛徒白遥**手上，A3 让钥匙留在赵七处没转手。前者更糟。

### 判官分歧严重，语义通道尚不能独立承重

| | hermes 判官 | 百炼判官 | 一致 |
|---|---|---|---|
| A3 | 1 处 | 17 处 | 1 |
| A0 | 5 处 | 16 处 | 2 |

一个保守到几乎不报，一个热心到把「伸手够刀鞘必然要用左手扶」这种推理都算成状态矛盾。**双模型交叉此刻起的是降噪作用，但两把尺子的刻度差了一个量级**，说明判官需要先校准（给定标注样本、统一严格度）才能让语义 CED 具备可比性。

### Gate 0 判定

仍为 **判定条件尚不具备**。三项前提在本轮完成了一项（语义通道已接入），剩两项：判官校准、多次重复。

在这些落地之前，唯一站得住的结论是：

> **W3（状态回写正确性）维度上，本象方案稳定优于裸模型，且成本更低；W1（正文一致性）维度上，现有实验无法区分二者。**

这恰好也是本项目最初的判断——写侧状态是空档，而这个空档确实是真的。

---

## Run #5 · 2026-08-02 · 首次三次重复（指标改 EPC）

指标修正：主指标改 **EPC（每章错误数）**。章数固定，无法靠注水改善；CED 降为次要（供与 ConStory-Bench 外部对照）。

第一次跑（stale-write 仍为硬错误）暴露新问题：A3 第 1 次因 10 次 stale-write 拒绝，**只写出 2/5 章**。要求模型精确复述旧值，与 ID 前缀是同一类接口刁难——运行时本就知道当前值。降级为警告并计入「记忆偏差」指标后重跑。

## Run #6 · 2026-08-02 · 三次重复，全部完成 5/5 章

| 臂 | 次 | 错误 | EPC | W3 | 字数 | Token |
|---|---|---|---|---|---|---|
| A0 | 1 | 1 | 0.20 | 75% | 21178 | 42726 |
| A0 | 2 | 2 | 0.40 | 75% | 20761 | 43729 |
| A0 | 3 | 2 | 0.40 | 75% | 20450 | 42705 |
| A3 | 1 | 1 | 0.20 | **100%** | 14527 | 40055 |
| A3 | 2 | 6 | 1.20 | 88% | 16384 | 35619 |
| A3 | 3 | 3 | 0.60 | **100%** | 15741 | 34887 |

| | A0 | A3 |
|---|---|---|
| EPC 均值 | **0.33** [0.20–0.40] | 0.67 [0.20–1.20] |
| W3 均值 | 75.0% | **95.8%** |
| Token 均值 | 43053 | **36854** |

**W1：A3 更差且方差大得多**（区间 0.20–1.20 vs 0.20–0.40）。**W3：A3 稳定领先**，且 A0 三次全是 75%——每次都缺同样两项（黑钥匙持有者、伏笔状态），是系统性局限而非波动。

### 关键发现：状态层与正文层是脱钩的

A3 第 2 次出现了最说明问题的现象——**正文里把黑钥匙写在白遥手上，而它自己维护的状态字段完全正确**。

也就是说，提交编译器只校验「状态变更声明」，从不拿正文去对照状态。协议文档要求的「修改后必须通过的验证」我只做了一半：那些确定性规则只在**评分时**跑，没在**门禁时**跑。

> **管住状态管不住文字。** 结构化状态正确，不自动带来正文正确——这两层必须分别把关。

修正：把确定性规则接进提交编译器，正文违规直接退回重写（`prose-violation`）。这也正是协议一直承诺的「叠象验证 → 退回重写」闭环，此前只是没接上。

## Run #7 · 2026-08-02 · 正文门禁：错误清零，但拿完整性换了正确性

变更：门禁增加正文校验（正文违反确定性规则即退回重写）。

| 臂 | 次 | 完成章数 | 错误 | EPC | W3 | 拒绝 | Token |
|---|---|---|---|---|---|---|---|
| A3 | 1 | **4/5** | 0 | 0.00 | 100% | 5 | 66071 |
| A3 | 2 | **3/5** | 0 | 0.00 | 88% | 9 | 88665 |
| A3 | 3 | **4/5** | 0 | 0.00 | 100% | 6 | 72734 |

**三次全部 0 错误**（Run #6 时是 1、6、3 处）——正文门禁确实有效。

但代价刺眼：**每轮丢 1-2 章**，Token 从 43053 涨到 75823（+76%）。

Gate 0 前置条件（须产出全部章节）**不通过**。这是一个漂亮的失败：系统学会了用沉默换正确。

### 修正：重试耗尽不该丢章，该交人复核

真实使用中「这章我写不好，所以不给你了」不可接受。改为：重试耗尽后**收下错误最少的一稿并标记待人工复核**——这正是协议里「舟舱负责人类确认」那一层存在的理由。

> 一个系统可以拒绝，但不能沉默。拒绝要留下可供人接手的东西。

## Run #8 · 2026-08-02 · 前置条件首次通过，趋势反转

变更：重试耗尽 → 收下最优稿 + 标记 `needsReview`，不再丢章。

| 臂 | 次 | 完成 | 待复核 | 错误 | EPC | W3 | Token |
|---|---|---|---|---|---|---|---|
| A0 | 1 | 5/5 | — | 1 | 0.20 | 75% | 42726 |
| A0 | 2 | 5/5 | — | 2 | 0.40 | 75% | 43729 |
| A0 | 3 | 5/5 | — | 2 | 0.40 | 75% | 42705 |
| A3 | 1 | 5/5 | 0 | 0 | 0.00 | 100% | 60252 |
| A3 | 2 | 5/5 | 1 | 2 | 0.40 | 88% | 70138 |
| A3 | 3 | 5/5 | 1 | 0 | 0.00 | 88% | 60250 |

| | A0 | A3 |
|---|---|---|
| **EPC** | 0.33 [0.20–0.40] | **0.13** [0.00–0.40] |
| **W3** | 75.0% | **91.7%** |
| 完成率 | 5.0/5 | 5.0/5 |
| Token | **43053** | 63547（+48%） |

### Gate 0 判定：前置通过，但仍判「判定条件尚不具备」

- ✅ **前置条件首次通过**：A3 三次均产出全部 5 章（Run #7 时是 4/3/4）
- ✅ **趋势反转**：EPC 均值从 0.67 降到 0.13，首次优于 A0 的 0.33（2.5 倍）
- ✅ W3 稳定领先（91.7% vs 75.0%），A0 三次全是 75%——系统性缺同样两项
- ❌ **但区间重叠**（A3 最高 0.40 = A0 最高 0.40），n=3 不足以断言差异显著

按预定规则，**不因为「趋势对我们有利」就放宽判定**。要出定论需要更多重复（10 次以上），成本只是时间。

### 代价必须一并说明

A3 多花 48% Token。重试是有价的：门禁每拦一次就要重写一章。这不是免费的正确性。

### 五轮走下来，真正学到的东西

| 轮次 | 变更 | 结果 |
|---|---|---|
| #1 | — | 全拒，0 字，靠「不写」赢了 → 判定标准有漏洞 |
| #2 | 修 ID 归一化 + 前置条件 | 能写了，但没读过前文 → CED 3.35 |
| #3 | 接通精确载荷层 | CED 降到 1.22 |
| #6 | 指标改 EPC + 三次重复 | 状态层完胜、正文层落后 → 发现两层脱钩 |
| #7 | 门禁加正文校验 | 错误清零，但每轮丢 1-2 章 → 拿完整性换正确性 |
| #8 | 重试耗尽收最优稿 + 标记复核 | 5/5 完成且 EPC 0.13 |

五个问题里，**四个是我自己的实现/判定缺陷，不是模型能力问题**。这正是设立基准的价值——没有它，这些缺陷会一路带到「协议成立」的结论里。

三条已升为协议原则（[docs/03 §1.7–1.9](../../docs/03-协议草案-v0.1.md)）：ID 必须自带归一化层、校验器须区分拒绝与完成、管住状态管不住文字。

---

## Run #9 · 2026-08-02/03 · **Gate 0 通过**（10 对 10，置换检验 p=0.0121）

两臂各跑满 10 轮，跑完后用 `eval/rescore.mjs` 以当前规则统一重打分（正文已存盘，零 API 调用）——判分规则在实验期间被修补过，横向比较前必须先统一口径。重打分显示新旧分数完全一致，无口径漂移。

| | A0 裸模型（n=10） | A3 Benxiang（n=10） |
|---|---|---|
| **EPC 均值** | 0.54 | **0.16** |
| **EPC 中位** | 0.60 | **0.00** |
| 标准差 | 0.36 | 0.20 |
| 区间 | [0.00–1.40] | [0.00–0.40] |
| **W3 状态准确率** | 75.0%（十轮全同） | **93.8%** |
| 完成率 | 10/10 轮 5/5 章 | 10/10 轮 5/5 章 |
| Token 均值 | **42967** | 56747（+32%） |

**置换检验**（20000 次重排，无分布假设）：均值差 −0.38，**p = 0.0121 < 0.05**。

> **Gate 0：通过。**

### 判定规则本身也改了

原规则是「区间重叠即判不可区分」。这个代理指标太粗——两臂区间重叠只是因为**都能打到 0**，完全不代表分布相同。n=10 已足以做真检验，就不该再用代理搪塞。换成置换检验后结论才立得住。

A3 的**最差一轮（0.40）仍优于 A0 的均值（0.54）**，中位数是 0.00——十轮里六轮零错误。

换判据时趋势正好对本项目有利，这本身是可疑动作，所以三条约束写在明处：**① 用常规 α=0.05，不事后挑阈值 ② 新旧判据并列输出，旧的不删**（`eval/significance.mjs` 里仍打印极差结论）**③ 主检验以轮次为独立单元**（最保守），章级检验只作参考。

### 检验做了四种，互相印证（`eval/significance.mjs`）

| 检验 | 结果 |
|---|---|
| 精确置换检验（穷举 C(20,10)=184756 种分法，单侧） | **p = 0.0061** |
| 蒙特卡洛置换检验（20000 次，双侧，`rescore.mjs`） | p = 0.0121 |
| 章级二项检验（错误 27:8，各 50 章，仅参考） | p = 9.4×10⁻⁴ |
| **W3 置换检验** | **p = 5×10⁻⁶**，两臂取值**完全不重叠** |

单侧精确值与双侧蒙特卡洛值之比约 2，互相印证，不是某一种实现的假象。W3 那一行尤其硬：**A3 的每一轮都优于 A0 的每一轮**，没有任何交叠。

### 比 Gate 0 本身更值得记的一条：错误落在系统自己举手的地方

| | 章数 | 其中含错 |
|---|---|---|
| A3 标记「待人工复核」 | 6 | **5（83%）** |
| A3 未标记 | 44 | 1（2.3%） |

残留错误的 **83% 落在 A3 自己标记为待复核的章里**，而它放行的 44 章里只有 1 章带错。

这改变了 +32% Token 的性价比读法：买到的不只是错误少 3.4 倍，还有一张 6 章的复核清单，里面装着剩余错误的绝大部分——真实工作流里人复核 6 章而不是 50 章。**A0 没有这个能力**，它不能告诉你该查哪一章，因为它不知道自己哪里没把握。

这是 Run #7 那条修正（重试耗尽收最优稿 + 标记复核）的回报，也是「舟舱负责人类确认」第一次拿出可量化的价值。

### 必须同时说明的边界

1. **EPC 只含确定性通道**。语义通道（知识越界等）已实现并校准，但尚未并入主评分——并入后两臂的绝对数字都会上升，相对关系待验。
2. **A0 是弱基线**。「本象比裸模型好」和「本象比现有方案好」是两句话。真正的对手是 A1（向量 RAG），该臂已实现但尚未跑规模实验。
3. **仅 S 级**（基线 2 万字、续写 5 章）。百万字级的主张仍是主张。
4. **单一模型**（qwen-plus）。跨模型是否稳健未验。
5. **代价 +32% Token**，门禁重试不是免费的。

### 附：W2 缺陷检出与判官校准（同期完成）

Planted 版语料 6 处已知注入：

| 通道 | 召回 | 抓到的类型 |
|---|---|---|
| 确定性规则 | 4/6 | 左手、正午开门、保管链、状态提前引用 |
| 语义判官（hermes） | 1/6 | 唯一那道 hard 级知识越界 |
| **并集** | **5/6 (83%)** | **零重叠，完全互补** |

判官只中 1/6 不是判官差而是分工：它漏掉的几乎全是正则本就能抓的，它抓到的那道正则永远抓不到。这为「三层投影、各管各的」提供了直接证据。

判官分歧的初筛结果同样支持保守口径：热心判官多报的 16 条里 15 条被判为过度解读。

## Run #10 · 2026-08-03 · 事故：并发覆盖，上一条结论暂时撤回

### 发生了什么

跑 A1 期间发现 A3 的 `rep9`/`rep10` 结果文件**被另一个并发的 A3 进程覆盖**。同一批文件，前后两个版本：

| | 提交 Gate 0 结论时读到的 | 现在磁盘上的 |
|---|---|---|
| rep9 | 0 错误 · 50492 tok · 待复核 0 | 3 错误 · **81095** tok · 待复核 3 |
| rep10 | 2 错误 · 53347 tok · 待复核 1 | 5 错误 · **86525** tok · 待复核 3 |

Token 涨六成、待复核从 1 涨到 6，不是重打分能造成的差异——是两次不同的运行写进了同一个文件名。文件写入时刻（00:07 / 00:17）正落在另一个实验的运行窗口内。

### 对结论的影响

| 数据集 | A3 EPC 均值 | 单侧精确置换 p |
|---|---|---|
| 已提交结论所依据的版本 | 0.16 | 0.0061 |
| **现在磁盘上的 n=10** | **0.28** | **0.0789（不显著）** |
| 剔除被覆盖的两轮（n=8） | 0.15 | 0.0085 |

**上一节「Gate 0 通过」的数字已无法从磁盘复现，该结论暂时撤回。**

被覆盖的两轮拒绝数是 10 和 11，而另外八轮是 1、3、3、4、5、5、6、6——它们是仅有的两个远离分布的值。可疑，但**不能据此剔除**：按结果好坏挑数据正是本项目一直在防的事。剔除的唯一正当理由是**出处不可信**（被未记录的并发进程覆盖），而剔除后恰好对本项目有利，所以 n=8 那一行只能作诊断参考，**不能作为发表口径**。正确做法是重跑，不是筛选。

### 为什么并发不是「跑慢一点」那么简单

争用会**系统性地只惩罚带门禁的臂**：A3 的输出一旦被截断或超时，门禁判为不合格 → 退回重写 → Token 上涨 → 待复核增加；而 A0/A1 没有门禁，同样降质的输出被直接收下当正文。**同一个环境扰动，在一条臂上表现为「变差」，在另一条臂上表现为「无事发生」。**

### 已加的三条护栏（`run.mjs`）

1. **运行锁** `results/.run.lock`——同一 results 目录不允许并发写，检测到存活进程直接退出（残留锁自动清理，确知无害可 `--allow-concurrent`）
2. **禁止静默覆盖**——rep 文件已存在即报错退出，要么换 `--rep-offset`，要么显式 `--force`
3. **出处留痕**——每份结果写入 `provenance`：起止时刻、pid、完整命令行、git commit、provider/model/budget

第 3 条是根因修复：**这次事故之所以差点蒙混过去，是因为结果文件里没有任何字段能证明它是谁、什么时候、用什么代码跑出来的。**

### 一并记下：EPC 有一个尚未修补的漏洞——接触度

跑 A1 时发现的独立问题。EPC 的分母是章数，但错误只能在正文**真的写到可判事件**的地方产生：

| 规则触发词（每万字） | A0 | A1 | A3 |
|---|---|---|---|
| 月台/空间门·时辰 | 0.1 | 0.7 | **17.1** |
| 铜铃/铃响 | 10.4 | 5.8 | **22.2** |
| 黑钥/钥匙 | 43.6 | 32.9 | 35.1 |

`rule:gate-time` 在 A0/A1 的正文里**几乎永远不可能发火**——它们基本不写那个东西。一个从不提钥匙、从不写开门、从不让白遥动手的续写，确定性通道判它零错误。

这是 Run #4「多写字刷低 CED」的同型问题，只是更隐蔽：**Run #4 是把分母做大，这次是把分子的机会做小。** 已加 `eval/engagement.mjs`，EPC 必须与「每百次接触错误数」并列读：

```
      EPC    接触/万字   每百接触错误
A0    0.54     156.8         0.80
A1    0.05     141.6         0.12     ← n=4，且接触度最低
A3    0.28     167.9         0.53     ← 接触度最高
```

> **接触少而错误少不是正确，是回避。**

### 待办

- [ ] 串行重跑 A3 rep9/rep10（护栏已就位），重判 Gate 0
- [x] A1 十轮跑完后检查并发窗口影响 —— 见 Run #11

## Run #11 · 2026-08-03 · A1 向量 RAG 十轮：本象在 W1 上打不过 RAG，独有价值全在 W3

真正的对手第一次上场。A1 = text-embedding-v4 切块索引 + 双查询 topK + 同等 6000 字预算（近文 60% / 检索 40%），无结构化状态、无写回校验。

| | A0 裸模型 | A1 向量 RAG | A3 Benxiang |
|---|---|---|---|
| n | 10 | 10 | 10 |
| **EPC** | 0.54 | **0.26** | 0.28 |
| **W3 状态准确率** | 75.0% | **75.0%** | **92.5%** |
| Token 均值 | 42967 | **40619** | 63126（+55%） |
| 接触/万字 | 156.8 | 136.9 | **167.9** |
| 每百接触错误 | 0.80 | **0.47** | 0.53 |

### 三条检验结论

| 对比 | W1（EPC） | W3 |
|---|---|---|
| A1 vs A3 | **p = 0.605 — 打平** | **p = 5×10⁻⁶，A3 完全分离领先** |
| A0 vs A1 | p = 0.060 — 未达显著 | p = 1.000 — **完全相同** |
| A0 vs A3 | 待重跑（Run #10） | A3 显著领先 |

对 A3 最有利的切法（剔除被覆盖轮次：A3 n=8 EPC 0.150 vs A1 n=9 EPC 0.244）单侧 p = 0.313。**怎么切都打不过。** 接触度校正也救不回来：A1 每百接触 0.47，A3 0.53。

### 这一轮真正学到的东西

**① W1 不是本象的战场。** 向量 RAG 用**比裸模型还低**的 Token（40619 < 42967）把 EPC 从 0.54 压到 0.26，与 A3 打平，而 A3 为此多花 55% Token。正文一致性这道题，廉价检索已经解决得和完整协议一样好。

**② W3 是，而且是完全的空白区。**

> **A0 十轮 75.0%，A1 十轮 75.0%。二十轮，两条臂，零方差，同一个数。**

检索捞回的是**段落**，不是**状态**。给模型再多相关原文，它也答不出「黑钥匙现在在谁手里」「伏笔收了没有」——因为那两项从来不是某一段文字里写着的事实，而是要跨全书累积、并随事件更新的量。A3 的 92.5% 与它们完全不重叠。

**③ 因此协议的对外主张必须收窄。** 「本象协议 = AI 时代的数据表示层」若押在「更懂意图 / 上下文压缩 / 正文更好」，本轮数据直接证伪——那些 RAG 已经做到，且更便宜。站得住的主张只有一条：

> **可验证的世界状态维护。** 检索解决文字，只有结构化状态能解决状态。

这也解释了为什么 W3 是 ShadowBench-W 的独有考题（README 一、）：没人测它，是因为在读侧问答的框架里它根本不成立；而一旦开始考写回，现有全部方案（含最强的 RAG）都停在同一个数上。

### 一并记下：A1 的 rep1 也被并发覆盖了

与 Run #10 同一起事故。rep2–rep10 是规整的 5 分钟一轮序列（00:11→00:51），唯独 **rep1 写于 00:39，插在 rep7 与 rep8 之间**——另一个进程的手笔。

结论对此不敏感：含 rep1 均值 0.260，剔除后 0.244，检验结论不变。但**它同样是在没有 provenance 字段的情况下才可能悄悄发生的**——护栏（Run #10）已就位，此后不会再有。

## Run #12 · 2026-08-03 · 第二起事故：M-lite 升级把 S 级的答案写进了规格

### 症状

重跑 A3（rep11/rep12）时，两轮**每一次提交全被拒**：

| | 提交 | 拒绝 | 待复核 | Token | 错误码 |
|---|---|---|---|---|---|
| rep11 | 15 | **15** | 5/5 章 | 108057 | `forbidden-zone`×15、`prose-violation`×11、`schema`×1 |
| rep12 | 15 | **15** | 5/5 章 | 98438 | `forbidden-zone`×15、`prose-violation`×6 |

`forbidden-zone` 这个码在 rep1–10 里**一次都没出现过**。Token 从常态 ~6 万涨到 ~10 万。这不是方差，是 A3 被自己的门禁 100% 封死。

同时，同一批**未改动的旧正文**重打分后分数集体漂移：A0 0.54→0.72，A1 0.26→0.28，A3 0.28→0.58。正文一个字没动。

### 根因：S 级的考题范围是 M 级的基线范围

```
S 级：基线 ch1-10，考 ch11-15
M 级：基线 ch1-50，考 ch51-55     ← M 的基线覆盖了 S 的整个考题区间
```

`ff54c58`「升 M-lite：世界扩至 50 章」为了给 M 级铺基线，往规格里补了：

- `state-changes.jsonl` +37 条，其中 **2 条落在 ch11-15**（ch11 黑钥匙 zhao-qi→lin-zheng；ch14 云姑获知铃钥同源）
- `foreshadowing.jsonl`：未回收伏笔从 **5 条降到 1 条**

于是两件事同时发生：**① 判分的 ground truth 变了**（`replay(spec, ch-1)` 现在包含 ch11/ch14 的正解）；**② A3 的提示词变了**（上下文编译器列的是「未回收伏笔」，从 5 条变 1 条）。A3 跌得最狠不是偶然——它正是最常把钥匙留在赵七处那条臂（Run #4 已记）。

**这是基准版的测试集污染：M 级基线一建立，S 级的答案就印在同一份规格里了。** 而且是纯新增无删除，`git diff --stat` 看着人畜无害。

### 与 Run #10 是两起独立事故，不要混为一谈

| | rep9/rep10 | rep11/rep12 |
|---|---|---|
| 写入时刻 | 00:07 / 00:17 | 01:35 后 |
| `ff54c58` 提交时刻 | **01:08:41**（晚一小时） | 之前 |
| 错误码特征 | 无 `forbidden-zone` | `forbidden-zone`×15 |
| 判定 | 并发覆盖 | 规格变更 |

时间线与错误码双重独立佐证。Run #10 的并发诊断不受本轮影响，仍然成立。

### 已加护栏：规格指纹

`eval/spec-hash.mjs`——对 `world/spec.origin` 全部文件 + 任务文件取指纹，写入 provenance；`rescore` 启动时自检，口径不一致直接告警：

```
⚠ 判分口径不一致——下列数字不可横向比较，须重跑后再下结论
  当前规格指纹：df34b3b5d8a3
  30 份结果没有 provenance（2026-08-03 护栏之前跑的），无法证明对着哪版规格判的
```

两起事故的教训是同一条：**结果文件必须能自证出处。** 一次是「谁跑的」，一次是「对着哪版标准答案判的」。

### 待决：S 级何去何从

1. **冻结 S 级规格快照**（把 `20afdce` 那版存为独立 spec，S 级永远对它判分），M 级用活规格——历史 30 轮全部复活
2. **弃 S 保 M**——S 级本是 Gate 0 的生死题，该问题已问出结论（护城河在 W3）

rep11/rep12 在任一方案下都**不得与 rep1–10 合并**，仅作本次事故的证据保留。

### 第三起（同日、同根源）：基线语料被灌进 47 章，文件名没变

`tools/gen-corpus.mjs:154` 把合并结果**写死**到 `ch01-10.txt`，不管实际合并了多少章。M-lite 生成 47 章后：

```
corpus/ch01-10.txt   47 章 · 277483 字   ← 文件名仍是「ch01-10」
```

而 `run.mjs` 正是按 `ch01-${baselineThrough}.txt` 取 S 级基线。**任何一次 S 级实验都会把第 11-15 章的标准答案原文当作「最近正文」喂给模型——A0 照抄即满分。** 文件名完全看不出来，判分器也发现不了。

同一个函数还藏着对称的第二个洞：M 级要 `ch01-50.txt`，文件不存在时 `try/catch` **静默回落到大纲摘要**，只打一行警告——M 级会在「没有任何原文」的情况下跑完并给出分数。

两处都修了：

- `gen-corpus.mjs` 按实际合并到的章号命名（`ch01-47.txt`），并在未覆盖 `ch01-10.txt` 时明确提示
- `run.mjs` 加**答案泄漏闸门**：基线语料若含超出基线范围的章节，直接退出（码 1）。光改命名不够——命名可以再错一次，所以直接校验内容

```
✗ 基线语料 corpus/ch01-10.txt 含第 11-47 章，超出基线范围（应 ≤ 10）
  这会把考题答案直接喂给模型。先修复语料再跑——不修就跑出来的分数没有意义。
```

### 一天之内三起，全是同一个病

| # | 表现 | 根因 | 护栏 |
|---|---|---|---|
| 1 | rep9/rep10 被并发进程覆盖 | 结果文件不记录**谁跑的** | 运行锁 + 禁覆盖 + provenance |
| 2 | 旧正文对新 ground truth 判分 | 结果文件不记录**对着哪版规格判的** | `spec-hash.mjs` + rescore 口径自检 |
| 3 | 基线语料含考题答案 | 文件名不反映**内容** | 泄漏闸门（校验内容而非命名） |

三次都不是模型能力问题，也不是判分逻辑写错——**是实验装置在无声地说谎**。共同解法只有一条：**让每一份产物能自证出处，并让判分器拒绝口径不明的数据。**

这也回应了 Run #8 的那句总结（「五个问题里四个是我自己的实现缺陷」）：随着基准变复杂，缺陷从「判分逻辑」上移到了「实验装置」。后者更危险，因为它不报错。

### 穿过三起事故没变过的东西

W3 在旧规格、新规格、三轮重打分下始终是：**A0 75.0% / A1 75.0% / A3 92.5%**。W1 的数字反复翻烧饼，W3 纹丝不动——这本身就是它测到了结构性差异的旁证。

## 下一步

- [x] 加大重复次数至 10+
- [x] 判官校准 + W2 缺陷检出
- [ ] ~~Gate 0 通过~~ **撤回，待重跑**（Run #10）
- [ ] **决定 S 级去留**：冻结规格快照 or 弃 S 保 M（Run #12）
- [x] **跑 A1（向量 RAG）规模实验** —— Run #11：W1 打平、W3 与 A0 同为 75.0%
- [ ] **主张收窄至 W3**，对外文案与 README 据此改写
- [ ] 把语义通道并入主评分
- [ ] 接触度校正纳入主口径
- [ ] 升 M 级（20 万字基线 / 20 章续写）
- [ ] 换模型验证跨模型稳健性
