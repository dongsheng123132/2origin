# 本机登录态地图（自动生成）

> 生成时间：2026-08-06T19:16:42.133Z · 生成器：`scripts/credential-map.mjs`
> **用途：新会话的 AI 开工前读这份，就知道各工具登录没登录、token 去哪取，不用重测。**
> ⚠️ 本文件不包含任何 token 值。取 token 请按「获取方式」列的路径/命令去读。

| 工具 | 状态 | 说明 | 获取 token |
|---|---|---|---|
| Vercel CLI | ⚠️ broken | auth.json 存在但 token 为空（从未登录成功） | `cat C:/Users/ZhuanZ/AppData/Roaming/com.vercel.cli/Data/auth.json` |
| GitHub CLI (gh) | ✅ ok | gh auth status 显示已登录（keyring 存储，见 .claude 凭据）。验证：gh auth status | `gh auth token` |
| ClawHub CLI | ✅ ok | 已登录（token 前缀 clh_z-…，registry https://cn.clawhub-mirror.com/） | `cat C:/Users/ZhuanZ/AppData/Roaming/clawhub/config.json` |
| SkillHub (skillhub.cn) | ✅ ok | 已登录（token 前缀 skh_e2…，handle user_abeb50e0，userId 619537，登录时间 2026-08-06T18:33:13Z） | `cat C:/Users/ZhuanZ/.skillhub/credentials.json` |
| Claude Code 凭据 | ✅ ok | claudeAiOauth 有 accessToken；其他条目 无 | `cat C:/Users/ZhuanZ/.claude/.credentials.json` |
| npm registry 认证 | ✅ ok | .npmrc 含认证凭据（registry 见文件） | `cat C:/Users/ZhuanZ/.npmrc` |
| 代理环境变量 | ✅ ok | HTTP_PROXY=http://127.0.0.1:7897 HTTPS_PROXY=http://127.0.0.1:7897 | — |

## 各工具补登方法

- **Vercel**：部署走 dashboard 连 GitHub，不需要 CLI 登录（见 `research/2026-08-07-上线交接单-2origin网站.md`）。
- **ClawHub**：`clawhub login` 或直接编辑 `config.json` 的 token。
- **SkillHub**：`python ~/.skillhub/skills_store_cli.py login --key skh_xxx`，或网页生成 token 后写入 `credentials.json`。
