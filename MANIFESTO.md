# 本象宣言

> **一源万影：保存本象，按需投影。**
>
> 「圣人立象以尽意」——《易传·系辞》

---

## 一、病根：我们把影子当成了本体

人类的信息史，是一部不断把世界压扁的历史。

```text
真实建筑 → CAD 模型 → 平面图 → PDF → 截图 → OCR 文字
真实企业 → 业务流程 → Excel 表 → 汇总数字 → 一段报告
真实思想 → 关系网络 → 文章结构 → 线性文字
```

每一步都更好传播，每一步都丢掉一些东西。这在人类之间是划算的——我们靠常识和经验，能从残缺的投影里还原出大致的原物。

**但 AI 现在读到的，正是这种压缩了七八轮的「影子的影子」。**

更糟的是，我们把最后那层影子当成了源文件。改 PDF、改截图、改摘要——改的都是投影，而真正的对象从未存在于任何一个文件里。

这不是「反对文字」。文字是人类发明的极高压缩率、强组合的表示方式，对法律条款而言文字本身就是本源。问题只在于**把投影误当成对象本身**：

| 领域 | 本源是什么 | 我们却在改什么 |
|---|---|---|
| CAD | 几何、约束、参数、装配 | 导出的平面图 |
| Excel | 公式与数据依赖 | 屏幕上的像素 |
| 小说 | 人物状态、时间线、伏笔图谱 | 一段一段的正文 |
| 软件 | 代码、状态、运行行为 | 截图 |

## 二、一条不可逆的界线

从三维模型生成二维图片是容易的。从二维图片恢复唯一的三维模型——**通常不可能**。无数不同的三维对象可以投出同一个影子。

```text
本象 → 投影      ✅ 确定、可重复
投影 ⇏ 唯一本象   ❌ 不可逆
```

这条界线决定了架构的方向：**不要试图从影子倒推本体，要把本体留住。**

PDF、图片、Markdown、EPUB 都不是源文件，它们是**投影**——由本象按需生成的缓存视图。删掉不心疼，因为随时能再投一次。

## 三、AI 不该交付终态，该交付事务

今天的 AI 直接吐出完整成品：四十页 PPT、几十万字正文、几百行图表配置。内容、格式、引用一起失控，错了只能整个重来。

本象协议要求另一种输出：

> **AI 只提交语义事务——改什么、依赖什么、断言什么。**
> **由确定性的编译器校验、落地、生成终态。**

AI 说「我把黑钥匙交给了沈砚，我保证赵七还活着」。校验器不听它保证，自己去查。查不过就带着证据退回重写。

这不是不信任模型，而是**把「说到」和「做到」分开记账**。

## 四、今天的证据

理念到此为止。下面是可核对的数字（[完整记录](benchmark/shadowbench-w/results-log.md)，[原始结果](benchmark/shadowbench-w/results/)全部入库）。

在长篇叙事的状态追踪上——「关键物品此刻在谁手里」「谁知道了那个秘密」「哪些伏笔还没收」：

| 模型 · 基线 | 本象 | 🛑 裸模型（已撤回） | 🛑 向量 RAG（已撤回） |
|---|---|---|---|
| qwen-plus · 2 万字（各 10 轮） | **92.5%** | 75.0%（sd 0） | 75.0%（sd 0） |
| qwen-plus · 9.5 万字（各 11 轮） | **98.9%** | 75.0%（sd 0） | 75.0%（sd 0） |
| deepseek-v4-flash · 9.5 万字 | *n=1：100.0%* ⏳ | 53.8% ± 32.6（n=10） | 32.1% ± 37.1（n=7） |

🛑 **对照臂那两列已于 2026-08-04 撤回，本表暂不可用作对比结论。**

我原先在这里写的是：「两条对照臂三十三轮全部 75.0%、标准差 0——不是『平均差一点』，是卡在同一堵墙上一步没动。」那句话是错的，而且错得很难看。

自查（[Run #18](benchmark/shadowbench-w/results-log.md)）发现：对照臂的状态靠一次**额外的探询调用**采集，而那次调用**不传对话历史**——提示词第一句「根据你刚写的内容」，送到了一个从未见过那些正文的模型面前。更糟的是提示词里那段 JSON 模板**把 8 个答案里的 5 个字面印在了题面上**，第 6 个填空数组即过，第 7 个（伏笔状态）模板里根本没问、而对照臂结构上永远拿不到，只剩第 8 个真正在考东西。

**6÷8 = 75.0%。** 那堵「标准差为 0 的墙」是模板自己的答案。

**恰恰是那个漂亮的零方差，本该是最大的警报**——真实模型在真实任务上二十轮给出同一个数，只有一种解释，就是它没在做那件事。我却把它当成了最硬的证据。

**本象那一列不受影响**（A3 不走探询，状态由自己的状态机维护并逐条带证据）。但在对照臂重跑齐之前，**任何「本象 vs 对照」的对比都不成立，包括对本项目有利的那些。**

**deepseek 那一行必须先按住。** 首轮冒烟测得裸模型 37.5%、RAG 75.0%，我据此写下过「本象把水平悬殊的两个模型拉到了同一高度」。十轮之后，那两个数字实为 53.8% 和 32.1%——**单轮值根本不代表分布**。该模型上对照臂呈双峰：多数轮次落在 75%，另有若干轮直接 0%。

本象臂正在跑向 n=10（首次多轮 8-03 中断于 17/30，8-04 已从 rep2 续跑；实时轮数见 [results-log](benchmark/shadowbench-w/results-log.md)，本页不逐轮更新）。在它跑满之前，「状态正确性来自架构而非模型能力」这句话**只有 qwen 上的证据**，跨模型部分尚不成立。

这条更正本身值得留在宣言里：**一个 n=1 的漂亮结果，和一个 n=1 的难看结果，同样不可信。** 我在前一版里犯的错不是造假，是把「没有解释空间」当成了「不需要样本量」。

还有两件事值得单说：

- **基线越长，本象自己的分越高**（2 万字 92.5% → 9.5 万字 98.9%）。这是 A3 臂内部的纵向对比，不涉及对照臂，**不受 Run #18 影响**。但「优势越大」这个说法要等对照臂重跑才能再用——差值的另一半现在是无效的。
- ~~**向量 RAG 三十三轮纹丝不动，检索找不回状态。**~~ **已撤回（Run #18）。** 原因比「RAG 没用」更难看：检索到的片段进的是**写章**那几次调用，而状态是靠另一次**不传历史**的探询调用采集的——**检索结果根本到不了被测量的那一刻**。不是检索没用，是检索压根没参与这场考试。这个主张可能仍然成立，但这个实验证不了它。
- **RAG 的作用不但是模型相关的，还可能是负的。** 在 qwen 上它毫无改善（75.0% → 75.0%）；在 deepseek 上十轮/七轮下来，它**比裸模型更差**（53.8% → 32.1%）。检索塞进去的相关片段，对一个本就不稳定的模型是额外的干扰源。此前那句「RAG 能把弱模型拉到 75% 那堵墙前」同样出自单轮，一并作废。
  > 🛑 **本条已随 Run #18 撤回。** 「RAG 毫无改善」有一个远更平淡的解释：检索到的片段进的是**写章**那几次调用，而 W3 是靠另一次**无历史**的探询调用测的——**检索结果根本到不了被测量的那一刻**。不是检索没用，是它压根没参与这场考试。重跑后再说。

## 五、为什么这不是一个小说工具

一个方言成功可能是巧合，**两个才是协议**。

协议的形状是「通用外壳 + 领域方言」：外壳只定义生命周期和最小结构，各领域自带 Schema，但共享同一套动作——

```text
inspect · render · query · act · diff · validate · commit · rollback
```

[参考实现](compiler/)的自测就是照这条线设计的：**同一份代码、零改动**，同时跑通一个叙事世界（人物、秘密、伏笔）和一份销售数据（没有人物、没有情节、没有伏笔）。

关键的一步是把约束从代码变成数据。实验里的约束原本是三个写死的类型，名字里都带着小说味（`knows_must_not_gain`、`hook_must_stay`）。现在收敛成六个通用谓词：

```text
equals · not_equals · contains · not_contains · range · unchanged
```

「主角不得知道这个秘密」和「营收字段不得为负」，在协议层是同一件事。

## 六、往哪里去

**眼下**：长篇小说方言。已在 9.5 万字基线上验证，目标是**百万字级**——人物状态、时间线、伏笔图谱、禁区约束全程不失忆，事务式写作。这是最难的一档：长、状态多、约束互相纠缠，而且错了立刻看得出来。

**接着**：把同一套外壳装到别的本源上。

| 方言 | 本源是什么 |
|---|---|
| **CAD / 三维** | 几何、约束、参数、装配 |
| **Office** | 文档结构、样式、引用关系 |
| **Chart / 数据** | 数据依赖与公式，而非渲染出的图 |
| **Memory** | 会话之上的项目世界状态 |

CAD 是特别值得做的一个：它的「投影不可逆」最直观——图纸永远不是模型。而它的约束天然是机器可判的（几何冲突、装配干涉、参数越界），正好落在本象最擅长的那一格。

**远处**：如果这条路走通，AI 时代的「文件」不该再是一份 `.docx` 或 `.pdf`，而应该是一个**能被打开、被看见、被定位、被修改、被复验的本象包**——人看投影，AI 改本象，系统证明改对了。

## 七、我们不主张什么

这个项目的可信度建立在把边界说清楚上。

- **不主张「写得更一致」。** 正文一致性与向量 RAG **无显著差异**（p=0.9905 / p=0.3361）。这半边廉价 RAG 已经拿走了。
- **不主张「更省 Token」。** 相反，**更贵**——qwen 上贵 25%，带长思维链的 deepseek 上贵 149%。
- **跨模型尚不成立。** 第二个模型（deepseek）上本象臂正跑向 n=10（8-03 中断于 17/30，8-04 从 rep2 续跑，四项指纹与 rep1 全同）；而对照臂那两组数（n=10 / n=7）已随 Run #18 作废，须重跑。该模型方差极大且对照臂呈双峰（多数 75%，若干 0%），单轮值不代表分布——首轮那组漂亮数字已按此更正。在本象臂跑满之前，跨模型部分没有结论。
- **没有生产级适配器，没有真实用户。** CAD、Office 目前只是方言登记表上的一行字。

[实验记录](benchmark/shadowbench-w/results-log.md)里完整写着**八起事故**，全部是自己查出来的，每起都附着此后加上的护栏——包括判分器拿 S 级答案判 M 级答卷、把**正确答案判成错**、并顺手给出一个「更动听的负面结论」的那一起。

最后两起不是装置故障，而是**读数的人出了错**：第七起把一个 n=1 的漂亮结果当成了结论；第八起更彻底——那个被写进首页、当作最硬证据的「标准差为 0」，其实是提示词模板把答案印在了题面上。**一个漂亮到没有方差的数字，本该是警报而不是证据。**

一个错误的负面结果，比一个错误的正面结果更难被质疑，因为它看起来像研究者在克制地自我否定。

我们把这些都记下来，是因为对一个基准而言，**仪器的诚实度就是全部资产**。

---

> 过去，人类把世界压缩成文档交给计算机。
> 未来，AI 直接理解世界的本象，再为人类生成所需要的投影。

**保存本象，按需投影。**
